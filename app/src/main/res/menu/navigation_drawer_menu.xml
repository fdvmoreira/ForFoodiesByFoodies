<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android">
    <group>

    </group>

    <!-- Activities section -->
    <item android:title="Activities">
        <menu>
            <group>
                <item
                    android:id="@+id/menu_item_reviews"
                    android:icon="@drawable/ic_rate_review_24"
                    android:title="@string/reviews" />
            </group>

        </menu>

    </item>

    <!-- Manage Users section -->
    <item android:title="Manage Users">
        <menu>
            <group>
                <item
                    android:id="@+id/menu_item_add_new_user"
                    android:icon="@drawable/ic_add_24"
                    android:title="New User" />
            </group>
        </menu>

    </item>

    <!-- Logout section -->
    <group
        android:id="@+id/menu_group_logout"
        android:menuCategory="container">
        <item

            android:id="@+id/menu_item_logout"
            android:icon="@drawable/ic_logout_24"
            android:title="@string/logout" />
    </group>

</menu>